## Building Marvin

You MUST use:

- `cargo build -q` to build
- `cargo test -q` to test
- `cargo fmt -q` to format all Rust code
- `cargo clippy -q` to lint all code

You MUST resolve any issues discovered during these processes.
